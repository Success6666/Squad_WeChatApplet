<view class="page">
  <view class="card">
    <view class="row space-between">
      <view class="title" style="font-size:30rpx;">报名名单</view>
      <view class="badge badge--brand">{{count}}人</view>
    </view>
    <view class="subtitle" style="margin-top: 10rpx;">管理员可见</view>
  </view>

  <view wx:if="{{loading}}" class="empty">加载中...</view>
  <view wx:elif="{{!list.length}}" class="empty">暂无报名</view>

  <view wx:for="{{list}}" wx:key="openId" class="card">
    <view style="font-weight:700;">{{item.steamNick}}</view>
    <view class="subtitle">Steam64: {{item.steam64Id}}</view>
    <view class="subtitle" wx:if="{{item.position}}">位置：{{item.position}}</view>
    <view class="subtitle" wx:if="{{item.onlineTime}}">在线：{{item.onlineTime}}</view>
  </view>
</view>
