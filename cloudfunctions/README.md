云函数目录：每个子目录都是一个可独立部署的云函数。

公共工具库：为了兼容微信云函数“按目录打包上传”的行为，每个需要的云函数目录内都有一份 `common.js`（内置公共方法）。
这样云端运行时不会再出现 `Cannot find module '../common'` 或 `Cannot find module 'squad-common'`。

部署时请对每个云函数目录右键「上传并部署（云端安装依赖）」。




